<?php
#return array(
#    'db' => array(
#        'username' => '<%= @database['username'] %>',
#        'password' => '<%= @database['password'] %>',
#    ),
#);

return array(
    'db' => array(
        <%= @database.map{|k,v| "'#{k}' => '#{v}'"}.join(",\n") %>
    )
);
